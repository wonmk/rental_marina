import 'package:flutter/material.dart';

class DetailMobil extends StatelessWidget{
  const DetailMobil({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: Buat detail page untuk setiap mobil
    return Scaffold(
      body: SizedBox(),
    );
  }
}